locals {
  region           = "{{ region | default ('eu-central-1')}}"
  customer_name    = "{{ project_owner | default('change_me')}}"
  customer_project = "{{ project_name | default('change_me') }}"
  aws_account_id   = "{{ aws_account_id | default('change_me') }}" # used only for assume role purpose
  release_version  = "{{ released_version | default('change_me') }}"
}
